# 作業計画

このディレクトリには、進行中または未着手で、roadmap より細かい手順が必要な作業計画だけを置く。

現在、進行中の作業計画はない。必要になった時だけ `plans/<topic>.md` を追加する。

- ファイル名に日付を入れない。
- 完了した計画書は削除する。
- 実装後の現在仕様は `docs/architecture/` または `docs/design.md` に反映する。
- 未完了の残課題は `docs/roadmap.md` に残す。
